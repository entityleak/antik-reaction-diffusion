{
  "version": 2,
  "buildCommand": "NODE_OPTIONS='--openssl-legacy-provider' npm run build",
  "outputDirectory": "dist",
  "framework": "nodejs",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
} 